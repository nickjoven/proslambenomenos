<!-- evidence: scripts/experiments/p7_derive.py, scripts/experiments/p7_registration.json, scripts/experiments/p7_flux.py, scripts/experiments/p7_results.json, scripts/experiments/p7_plots.py, scripts/verify/p7_golden_flux.py -->
# P-7: the golden flux ladder - the address, the plateau, and the clock

Symbols: see the VOCABULARY.md symbol graph - phi is the ADDRESS
(the flux), Catalan's 32G/pi is the plateau (imported, LC-14), and
ln phi is now an EARNED growth edge: the Fibonacci-approximant clock
of the critical Harper bandwidth.

Registered before computing (PREDICTIONS.md P-7 - the number
reserved since A-1, worded 2026-08-27); resolution R-20, all four
clauses, mind-change quiet. Figure page p7_plots.html (anchors,
butterfly, plateau, clock), published as an artifact.

## The construction, validated before use

Almost-Mathieu at lambda = 1 on flux p/q: the q x q Bloch matrix
with corner +-1 and k2 in {0, pi/q} (the two Chambers extremes);
bands = consecutive pairs of the 2q sorted edge eigenvalues; cyclic
Jacobi with residual guard. Validated at registration against two
CAS closed forms: q = 2 gives E^2 = 4 cos^2 k2 + (1+s)^2 - bands
+-[0, 2 sqrt 2] touching at E = 0, which is catalog c25's pi-flux
Dirac point by the same number 2 sqrt 2; q = 3 gives
tr M + 2 cos 3 theta = E^3 - 6E with edges from integer
factorizations. Only after both anchors landed at 1e-10 did the
ladder run.

## What the ladder returned (R-20)

- Parity rule, ladder-wide: every odd-q gap open; the even-q
  central gaps (q = 34, 144) closed to 1e-14 - the Dirac touch
  persists along the even rungs.
- The plateau: q S(q) = 9.2509, 9.3199, 9.3608, 9.3208, 9.3290,
  9.3330 along q = 13..144, terminal-pair mean 9.3310 against the
  imported 32 G/pi = 9.32995 - 0.0011 away, with G computed from
  its own accelerated series at registration. The band was 0.25;
  the physics used 0.4 percent of it.
- The clock: ln(S(89)/S(144)) = 0.48075 vs ln phi = 0.48121,
  deviation 4.6e-4. Decomposition as registered: the Fibonacci
  ratio ln(144/89) supplies 0.48119 of it; the Thouless flatness
  supplies the residual correction. So the golden log is earned as
  CLOCK x FLATNESS - phi never appears in the invariant itself.

## Why this line matters to the taxonomy

The phi taxonomy (VOCABULARY.md) admits phi as address and refuses
it as fitted constant; ln phi sat in the unearned bin with P-7
named as the queued door. The door is now walked through, and the
result is exactly the taxonomy's shape: the golden number tells you
WHERE (the flux) and HOW FAST the ladder climbs (the clock), while
the invariant waiting at the end belongs to Catalan. A numerology
would have put phi in the plateau; the computation put it in the
clock and the address, and the silver-clock mutant dies by 0.4 nat.

Not claimed: the Cantor structure of the irrational limit (Ten
Martini, imported); the Thouless limit itself (imported - only the
approach is measured); Chern labels; lambda away from criticality;
dimensionful statements.
