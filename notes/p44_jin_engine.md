<!-- evidence: scripts/experiments/p44_spotcheck.py, scripts/experiments/p44_ellipse.py, scripts/experiments/p44_gramian.py, scripts/experiments/p44_results.json -->
# P-44 working document: the Jin engine, corroborated

The owner asked whether the connective tissue existed to promote
the Crouzeix survey remarks or whether more formalization was
needed. The answer this line implements: the tissue suffices for a
CORROBORATION-scope registered line - the survey agent's
verification layer (committed here as p44_spotcheck.py /
p44_ellipse.py, provenance in the 2026-09-01 session record)
becomes the derive layer, its measured values become the pins, and
the claim's scope says plainly what it is not. Promotion of "the
proof is correct" would need formalization we do not own: the
mass-m generalization in Lean (the audited repo covers mass 2),
and a conjugation-placement check against the actual PDF glyphs
rather than the survey's reconstruction. Those stay listed in
LC-33 as the open remainder.

## What the clauses established

Mass two exact on two domains; Caratheodory; adjoint-algebra
membership discriminated at ten orders (ellipse - the disk
trivializes it, the survey's own trap now a registered design
choice); Psi-cancellation at 1e-11; the Gramian chain clean over
400 legitimate instances; exact tightness from the correct side
along the extremal family. One legitimate violation anywhere would
have refuted Theorem 2 + Proposition 1 jointly.

## The instrument story

The runner's first draft substituted its own near-Jordan family
([[0,1],[0,eps]] - numerical radius 1/2 + O(eps)) for the layer's
pinned one ([[eps,1],[0,-eps]] - radius 1/2 + O(eps^2)) and fired
clause (d) at ||T|| = 1.980 against the >= 1.999 band. An L-9
repeat committed INSIDE the line that consulted L-9 - the lesson
ledger tells you to anchor, it cannot make you copy the anchor's
construction. Restored to the pinned family; the approach to 2
then matches the survey (1.9996, slack 4.0e-8).

## Status context (LC-33)

Two independent proofs (Jin; Lorist-Schwenninger), expert
endorsement including Crouzeix himself, the audited Lean covering
the load-bearing endpoint - and still unrefereed. The repo's
posture: engine corroborated, status imported, completely-bounded
conjecture open, audit repo's ranking call unchanged.
