id: jin-engine-corroborated
statement: >
  The new intermediate objects of Jin's claimed Crouzeix proof
  (preprints202607.1919.v4) verify numerically under this repo's
  discipline: the numerical-range double layer has mass two on
  disk and ellipse domains (defects at 1e-14); the completion
  H(w) is Caratheodory with its defect confined to the adjoint
  algebra, discriminated at ten orders of magnitude on a
  non-circular domain (the disk trivializes this test - recorded);
  the exact sampling cancellation holds at 1e-11; the
  ordered-Gramian chain I <= P~ <= 2I with its k = 1 term is
  clean over 400 seeded legitimate instances; and the chain is
  exactly tight, from the correct side, along the near-Jordan
  extremal family (||T|| -> 2 with slack 9.7e-3 -> 4.0e-8). A
  single legitimate violation would have refuted the manuscript's
  Theorem 2 + Proposition 1 jointly; none occurred. SCOPE: this
  corroborates the manuscript's engine and nothing more - the
  proof's status (endorsed, twice-proved, unrefereed) is imported
  in LC-33, and the completely bounded conjecture remains open.
premises: []
prediction: P-44
evidence:
  - kind: computation
    ref: "scripts/experiments/p44_spotcheck.py and p44_ellipse.py (the fresh-context survey agent's verification layer, committed as provenance); scripts/experiments/p44_gramian.py with p44_results.json (the registered clauses, seed 20260901); independently reimplemented by scripts/verify/p44_gramian_chain.py with closed-form 2x2 eigenvalues, a literal fixed instance, and its own scalar-mass quadrature"
    method: reimplementation
null_system: "instances must be LEGITIMATE (G = S*S, lambda_i = f(beta_i), |f| <= 1 on a domain containing W(B)) - for arbitrary (G, lambda) the chain is false, and the two mutants (sup-blind, mass-blind) break it deterministically on the extremal instance; the membership clause runs on the ellipse because the disk silently trivializes the adjoint-algebra defect; the runner's family-substitution error fired clause (d) once and was restored to the pinned instrument (R-43, an L-9 repeat)"
falsifier: "scripts/verify/p44_gramian_chain.py --mutant mass-blind"
novelty:
  status: classical
  citation: "everything mathematical is Jin's (and Lorist-Schwenninger's); the contribution is the registered, mutated, falsifiable corroboration layer - repo discipline applied to a live conjecture-resolution, not new mathematics"
status: verified
