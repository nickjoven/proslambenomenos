<!-- evidence: scripts/experiments/p22_derive.py, scripts/experiments/p22_registration.json, scripts/experiments/p22_locking.py, scripts/experiments/p22_results.json, scripts/experiments/p23_derive.py, scripts/experiments/p23_registration.json, scripts/experiments/p23_protect.py, scripts/experiments/p23_results.json, scripts/verify/p22_locking_skeleton.py -->
# P-22/P-23: locked references - the reference wave, computed

Registered before computing (PREDICTIONS.md P-22 with amendment
P-22a; P-23 with P-23a); resolutions R-17 (mind-change FIRED on two
clauses, diagnosed, not rescued) and R-18 (all cells held, with the
R-18a transcription correction). This is the A-10 heading: the
constructive answer to the "universal reference wave" claims killed
across this week's audits. PRB 111, 184519 (Danner et al. 2025)
stabilizes dc-biased Josephson-photonic squeezed microwaves with a
weak injected reference and names the Adler/Kuramoto class as the
frame (LC-13); here that named skeleton is reproduced with every
anchor derived in advance.

## What held (the claims)

1. two-photon-locking-z2-skeleton (P-22 clauses b, c, d): the
   sin 2 theta equation - the paper's twice-frequency injection -
   shares the fundamental Adler tongue exactly and carries a Z2
   DOUBLET of locked phases pi apart. Noisy drift on six pinned
   cells lands on the constant-flux Fokker-Planck quadrature
   (validated beforehand on four exact limits); locked variance
   follows the von Mises quadrature; and the phase hops between the
   two locked values at the exact mean-first-passage rate - 1337
   committed hops observed, every single one of size pi.
2. locking-protects-mod-pi-observable (P-23): <cos 2 theta> sits on
   I1(eps/2D)/I0(eps/2D) - computed with the SAME Bessel
   continued-fraction instrument P-15 built, including its
   stop-at-k=2 lesson - while pi-hopping drives <cos theta> to its
   derived null. The reference pins exactly the observable that is
   defined mod pi: the classical skeleton of "phase locking
   preserves the entanglement between photons".

## What fired, and what the firing taught (R-17)

- Clause (a) died at the two tongue-EDGE cells only: the winding
  count of a finite run is quantized, bounding the beat estimator
  by 2 pi/T - bigger than the registered band precisely where the
  beat is slow. All twelve non-edge cells passed at 2e-5 to 3e-4.
- Clause (e) died because the registered threshold 0.8 was taste,
  not derivation: the measurement landed 9e-4 from the DERIVABLE
  equilibrium I1(2)/I0(2) = 0.69777. P-23 re-registered exactly
  that number on fresh seeds and it held at 0.24-1.23 SEM.
- Bookkeeping: P-22a caught the mobility-band infeasibility BEFORE
  the run (the delta = 0 cell had a zero-width band); the winding
  quantization was the same error class not caught in time. And
  two ledger transcription slips in one line (P-23a, R-18a) earn
  the process rule recorded in R-18a: paste ledger numbers from
  artifacts, never retype.

## Why this line exists (the audit connection)

The week's audits kept meeting "reference wave" claims with no
mechanism, no observable, no tongue (the 5D Klein vacuum, the
zero-point decoder, the golden-mean condensate). This pair of
lines is the contrast object: a reference wave that DOES something
has an Adler equation, a tongue whose width is the injection
amplitude, a locked-phase multiplicity fixed by the injection
harmonic, hop statistics pinned by first-passage quadrature, and a
protected observable whose equilibrium is a Bessel ratio - every
number derived before it was measured. The instrument lineage runs
c25 (Adler anchor) -> P-19 (ALF locks, on the pending alf-v2
branch) -> P-15's Bessel machinery -> here.

Not claimed: any quantum observable (squeezing spectra,
entanglement measures, photon statistics), the paper's device
coefficients, multi-mode or mutual synchronization, relaxation
times, dimensional units.
